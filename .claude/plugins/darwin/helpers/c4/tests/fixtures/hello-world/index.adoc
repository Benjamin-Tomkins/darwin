= Hello World Plan

:phase: spec

[source,structurizr]
----
workspace "hello-world" {
  model {
    softwareSystem "Greeter" "A simple greeting service" {
      properties {
        identifier greeter
        impl greeter-impl.adoc
        tests greeter-tests.adoc
      }
      container "API" "The HTTP API layer" {
        properties {
          identifier api
          impl api-impl.adoc
        }
      }
    }
  }
}
----

== Overview

A minimal plan for smoke-testing the Darwin Ralph loop controller.
